CREATE TABLE  "ANNOUNCEMENTS" 
   (	"ANNOUNCE_ID" NUMBER NOT NULL ENABLE, 
	"SUBJECT" VARCHAR2(200) NOT NULL ENABLE, 
	"BODY" VARCHAR2(4000) NOT NULL ENABLE, 
	"ANNOUNCER_ID_FOREIGN" NUMBER NOT NULL ENABLE, 
	"DATE__OF_ANNOUNCEMENT" DATE, 
	 CONSTRAINT "ANNOUNCEMENTS_PK" PRIMARY KEY ("ANNOUNCE_ID") ENABLE
   )
/
CREATE TABLE  "PADHAILIKHAI_USERS" 
   (	"USERNAME" VARCHAR2(100) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(100) NOT NULL ENABLE, 
	 CONSTRAINT "PADHAILIKHAI_USERS_PK" PRIMARY KEY ("USERNAME") ENABLE
   )
/
CREATE TABLE  "PADHAILIKHAI_USERINFO" 
   (	"USERID" NUMBER NOT NULL ENABLE, 
	"USERNAME_FOREIGN" VARCHAR2(100) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(100) NOT NULL ENABLE, 
	"ADDRESS1" VARCHAR2(100) NOT NULL ENABLE, 
	"ADDRESS2" VARCHAR2(100), 
	"ADDRESS3" VARCHAR2(100), 
	"CITY" VARCHAR2(25) NOT NULL ENABLE, 
	"STATE" VARCHAR2(25) NOT NULL ENABLE, 
	"COUNTRY" VARCHAR2(25) NOT NULL ENABLE, 
	"USERTYPE" VARCHAR2(10) NOT NULL ENABLE, 
	"FIRSTNAME" VARCHAR2(50) NOT NULL ENABLE, 
	"MIDDLENAME" VARCHAR2(50), 
	"LASTNAME" VARCHAR2(50), 
	 CONSTRAINT "PADHAILIKHAI_USERINFO_PK" PRIMARY KEY ("USERID") ENABLE, 
	 CONSTRAINT "PADHAILIKHAI_USERINFO_FK" FOREIGN KEY ("USERNAME_FOREIGN")
	  REFERENCES  "PADHAILIKHAI_USERS" ("USERNAME") ENABLE
   )
/
CREATE TABLE  "SUBJECT_TABLE" 
   (	"SUBJECT_ID" NUMBER NOT NULL ENABLE, 
	"SUBJECTNAME" VARCHAR2(50) NOT NULL ENABLE, 
	 CONSTRAINT "SUBJECT_TABLE_PK" PRIMARY KEY ("SUBJECT_ID") ENABLE
   )
/
CREATE TABLE  "QUESTIONPAPER_LIST" 
   (	"TESTID" NUMBER NOT NULL ENABLE, 
	"SUBJECTNAME" VARCHAR2(25) NOT NULL ENABLE, 
	"FACULTYID" NUMBER NOT NULL ENABLE, 
	"NUMBEROFQUESTIONS" NUMBER NOT NULL ENABLE, 
	"MAXMARKS" NUMBER NOT NULL ENABLE, 
	"DATEOFUPLOAD" DATE, 
	"SUBJECT_ID_FOREIGN" NUMBER, 
	 CONSTRAINT "QUESTONPAPER_LIST_PK" PRIMARY KEY ("TESTID") ENABLE, 
	 CONSTRAINT "QUESTONPAPER_LIST_FK" FOREIGN KEY ("FACULTYID")
	  REFERENCES  "PADHAILIKHAI_USERINFO" ("USERID") ENABLE, 
	 CONSTRAINT "QUESTIONPAPER_LIST_CON" FOREIGN KEY ("SUBJECT_ID_FOREIGN")
	  REFERENCES  "SUBJECT_TABLE" ("SUBJECT_ID") ENABLE
   )
/
CREATE TABLE  "COMPLETED_TESTS" 
   (	"TESTID_FOREIGN" NUMBER NOT NULL ENABLE, 
	"USERID_FOREIGN" NUMBER NOT NULL ENABLE, 
	"SCORE" NUMBER, 
	"CORRECT" NUMBER, 
	"INCORRECT" NUMBER, 
	 CONSTRAINT "COMPLETED_TESTS_PK" PRIMARY KEY ("TESTID_FOREIGN", "USERID_FOREIGN") ENABLE, 
	 CONSTRAINT "COMPLETED_TESTS_CK1" CHECK (score>=0
) DISABLE, 
	 CONSTRAINT "COMPLETED_TESTS_CK2" CHECK (correct>=0) ENABLE, 
	 CONSTRAINT "COMPLETED_TESTS_CK3" CHECK (incorrect>=0) ENABLE, 
	 CONSTRAINT "COMPLETED_TESTS_FK" FOREIGN KEY ("TESTID_FOREIGN")
	  REFERENCES  "QUESTIONPAPER_LIST" ("TESTID") ENABLE, 
	 CONSTRAINT "COMPLETED_TESTS_FK2" FOREIGN KEY ("USERID_FOREIGN")
	  REFERENCES  "PADHAILIKHAI_USERINFO" ("USERID") ENABLE
   )
/
CREATE TABLE  "PADHAILIKHAI_FAQ" 
   (	"QUESTIONS" VARCHAR2(4000), 
	"ANSWERS" VARCHAR2(4000)
   )
/
CREATE TABLE  "QUESTION_TABLE" 
   (	"TESTID_FOREIGN" NUMBER NOT NULL ENABLE, 
	"QUESTION" VARCHAR2(4000) NOT NULL ENABLE, 
	"OPT1" VARCHAR2(500) NOT NULL ENABLE, 
	"OPT2" VARCHAR2(500) NOT NULL ENABLE, 
	"OPT3" VARCHAR2(500) NOT NULL ENABLE, 
	"OPT4" VARCHAR2(500) NOT NULL ENABLE, 
	"CORRECTANSWER" NUMBER NOT NULL ENABLE, 
	 CONSTRAINT "QUESTION_TABLE_FK" FOREIGN KEY ("TESTID_FOREIGN")
	  REFERENCES  "QUESTIONPAPER_LIST" ("TESTID") ENABLE
   )
/
CREATE TABLE  "USER_SUBJECT_TABLE" 
   (	"USER_ID_FOREIGN" NUMBER NOT NULL ENABLE, 
	"SUBJECT_ID_FOREIGN" NUMBER NOT NULL ENABLE, 
	 CONSTRAINT "USER_SUBJECT_TABLE_PK" PRIMARY KEY ("USER_ID_FOREIGN", "SUBJECT_ID_FOREIGN") DISABLE, 
	 CONSTRAINT "USER_SUBJECT_TABLE_FK" FOREIGN KEY ("USER_ID_FOREIGN")
	  REFERENCES  "PADHAILIKHAI_USERINFO" ("USERID") ENABLE, 
	 CONSTRAINT "USER_SUBJECT_TABLE_FK2" FOREIGN KEY ("SUBJECT_ID_FOREIGN")
	  REFERENCES  "SUBJECT_TABLE" ("SUBJECT_ID") ENABLE
   )
/

Rem No function found to generate DDL.

CREATE UNIQUE INDEX  "SUBJECT_TABLE_PK" ON  "SUBJECT_TABLE" ("SUBJECT_ID")
/
CREATE UNIQUE INDEX  "QUESTONPAPER_LIST_PK" ON  "QUESTIONPAPER_LIST" ("TESTID")
/
CREATE UNIQUE INDEX  "PADHAILIKHAI_USERS_PK" ON  "PADHAILIKHAI_USERS" ("USERNAME")
/
CREATE UNIQUE INDEX  "PADHAILIKHAI_USERINFO_PK" ON  "PADHAILIKHAI_USERINFO" ("USERID")
/
CREATE UNIQUE INDEX  "COMPLETED_TESTS_PK" ON  "COMPLETED_TESTS" ("TESTID_FOREIGN", "USERID_FOREIGN")
/
CREATE UNIQUE INDEX  "ANNOUNCEMENTS_PK" ON  "ANNOUNCEMENTS" ("ANNOUNCE_ID")
/
